# ICI retrieval pipeline — Stage 1 & 2 dependencies only.
# ML packages (sentence-transformers, chromadb, torch, cross-encoders) are
# deliberately NOT here yet — they arrive with Stage 3. Keep this file minimal
# so the Stage 1/2 environment stays fast to build and easy to reason about.

pandas>=2.2
pyarrow>=15.0
sqlite-utils>=3.36
fastapi>=0.110
uvicorn>=0.29

# Test infrastructure for the Stage 2 accuracy gate (not ML; required to run
# the pytest suite and exercise the FastAPI endpoints via TestClient).
pytest>=8.0
httpx>=0.27